import { FitrarRutaPipe } from './fitrar-ruta.pipe';

describe('FitrarRutaPipe', () => {
  it('create an instance', () => {
    const pipe = new FitrarRutaPipe();
    expect(pipe).toBeTruthy();
  });
});
