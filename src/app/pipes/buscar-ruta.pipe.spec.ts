import { BuscarRutaPipe } from './buscar-ruta.pipe';

describe('BuscarRutaPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarRutaPipe();
    expect(pipe).toBeTruthy();
  });
});
